#include <stdio.h>




void increase(int *x) {
    (*x)++;
}

int main() {
    int num;
    printf("Enter a number: ");
    scanf("%d", &num);

    increase(&num);  // pass address
    printf("Value after increase: %d\n", num);

    return 0;
}